{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m pip install uv && cd apps/api && python -m uv sync --frozen && python -m uv run alembic upgrade head",
    "watchPatterns": ["apps/api/**", "packages/types/**"]
  },
  "deploy": {
    "startCommand": "cd apps/api && python -m uv run uvicorn diorama_api.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
